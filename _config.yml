# Site settings
title: Network Scanning Probe Attack and Detection
description: This lab explores network scanning probes, a common tactic used in hacking attempts, and demonstrates how Wazuh can detect them using Suricata IDS (Intrusion Detection System).
author: 
  name: Jarrale Butts
url: "https://NSPDA.github.io"

# Markdown options
markdown: kramdown

# Theme settings (Jekyll theme)
#theme: jekyll-theme-hacker

remote_theme: pages-themes/hacker@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
